node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Every .env variant, not just the two that were listed: a copy saved as
# .env.bak or .env.production holds the same secrets as .env does.
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts

# Claude Code local settings — can contain command snippets with secrets
.claude/settings.local.json
